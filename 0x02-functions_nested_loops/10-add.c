#include <stdio.h>
#include "main.h"
/**
 * add - Prints the times table of 9
 *
 * Return: nothing
 * @a : the variabe
 * @b  : second variable
 *
 */
	int add(int a, int b)
{
	int results;

	results = a + b;

	return (results);
}
